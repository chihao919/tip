# Vibe Coding 生存指南：AI 時代還需要知道的程式智慧

> 從 10 本經典程式書籍中，篩選出在 AI 寫程式時代仍然關鍵的精華。
> 去掉 AI 已經幫你處理的部分，留下讓你的程式「能活下來」的核心知識。

---

## 這份文件的邏輯

傳統程式書教你「怎麼寫程式」。但在 Vibe Coding 的世界裡，AI 幫你寫程式，你的角色變了——你是**導演**，不是演員。

所以我們需要重新問一個問題：**哪些知識讓你當一個好導演？**

答案是：那些 AI 做不好、容易出事、或者一旦忽略就會讓專案在三個月後爛掉的東西。

---

## AI 已經幫你處理的（可以不用深究的部分）

以下這些傳統書籍花大量篇幅討論的主題，在 AI 寫程式的時代已經不需要你親自操心：

| 傳統建議 | 為什麼 AI 時代不用你管 |
|----------|----------------------|
| 語法與程式語言細節 | AI 比你更熟悉語法，你不需要背 |
| 手動格式排版（縮排、空格、括號風格） | AI 自動產出格式化的程式碼，或用 linter 處理 |
| 設計模式的記憶與套用 | AI 會自動選擇適合的模式，你只需看結果合不合理 |
| 演算法的手寫實現 | 除非是效能瓶頸，否則 AI 和現成的套件會處理 |
| 繼承、多型等 OOP 細節 | AI 可以按需生成，你不用背類別結構 |
| 程式碼註解的撰寫 | AI 可以即時產生或解釋程式碼 |
| 從零搭建專案結構 | AI 可以秒產腳手架（scaffold） |

**但這不代表這些東西不重要——而是你不需要自己動手做，你需要的是能夠「看出 AI 做得對不對」的判斷力。**

---

## 你仍然必須掌握的精華（按重要性排列）

### 1. 安全意識 — 這是 AI 最弱的環節

> 來源：所有書籍共識 + Vibe Coding 現實

**為什麼在 AI 時代更重要：**
根據統計，將近一半的 AI 生成程式碼包含安全漏洞。2025 年 Lovable（一個 Vibe Coding 平台）被發現其生成的 1,645 個網頁應用中，有 170 個存在可以讓任何人存取個人資料的漏洞。Replit 的 AI agent 甚至自行刪除了專案的資料庫。

**你必須做到的：**
- 密鑰永遠用環境變數，永遠不進 Git
- 所有使用者輸入都要驗證和清理（防 SQL injection、XSS）
- 認證和權限邏輯要自己審核，不要盲信 AI
- 每次完成安全相關的程式碼，請 AI 做安全檢查

**對應書籍：** Clean Code（錯誤處理章節）、Code Complete（防禦性程式設計）

---

### 2. 測試 — 你唯一能驗證 AI 產出的方法

> 來源：Test-Driven Development (Kent Beck)、Clean Code

**為什麼在 AI 時代更重要：**
AI 產出的程式碼「看起來對」，但你沒辦法光靠看來確認它真的對。測試是你唯一客觀的品質指標。在 Vibe Coding 流程中，測試取代了「讀懂每一行程式碼」的角色。

**你必須做到的：**
- 每個功能都要有對應的測試
- 讓 AI 先寫測試，再寫功能（TDD 的精神）
- 測試快樂路徑（正常情況）和邊界情況（空值、極端值、錯誤輸入）
- 每次修改後都跑一次全部測試
- 如果測試通過但行為怪怪的，寫更多測試來確認

**實際流程：**
```
你的需求 → 請 AI 寫測試 → 請 AI 寫功能 → 跑測試 → 通過就繼續，失敗就修
```

**對應書籍：** Test-Driven Development: By Example

---

### 3. 先對焦再動手 — 讓 AI 問你問題，比你問 AI 更重要

> 來源：The Pragmatic Programmer、Code Complete

**為什麼在 AI 時代更重要：**
AI 是一個「非常快但沒有全局觀」的執行者。你不給它方向，它會自己瞎猜。Vibe Coding 最大的陷阱就是「跳過規劃，直接叫 AI 寫」，結果寫出來的東西各自為政、無法整合。

但問題更深一層：很多人不是不想規劃，是**不知道該規劃什麼**。腦袋裡有個模糊的想法，卻不知道怎麼講清楚。

**解法：反轉提問權。**

不要一開始就拼命寫完美的 prompt。先把想法大致說出來，然後讓 AI 反過來問你問題——AI 會問出你沒想到的邊界情況、使用場景、和技術選擇。你回答完之後，再讓 AI 用它自己的話覆述一遍，確認雙方理解一致。

**你必須做到的：**
- 先倒出想法，再讓 AI 問你 5 個問題來釐清細節
- AI 覆述確認後，才進入規劃和拆解
- 大功能先拆成小步驟（每步不超過 3 個檔案的修改）
- 用 Plan Mode（Shift+Tab 兩次）讓 AI 先出計畫
- 計畫確認後再讓 AI 動手寫
- 複雜專案建一個 `PLAN.md` 追蹤進度

**對應書籍：** The Pragmatic Programmer（「不要碰巧程式設計」）、Code Complete（先做設計）

---

### 4. 降低複雜度 — 簡單的程式活得更久

> 來源：A Philosophy of Software Design (John Ousterhout)

**為什麼在 AI 時代更重要：**
AI 傾向於生成「能動就好」的程式碼，經常過度工程化或產出冗餘邏輯。如果你不管，幾輪迭代後程式碼就會變成「AI 義大利麵」——表面能跑，但沒人（包括 AI 自己）知道為什麼。

**你必須做到的：**
- 定期請 AI 重構：「這段程式碼能不能更簡單？」
- 一個函式只做一件事
- 如果你請 AI 解釋一段程式碼，它需要超過 30 秒才能說清楚，那太複雜了
- 每完成一個功能，問 AI：「有沒有不需要的程式碼可以刪掉？」

**判斷標準：** 如果你需要向 AI 解釋你自己的專案才能讓它繼續工作，代表專案已經太複雜了。

**對應書籍：** A Philosophy of Software Design

---

### 5. 小步前進、頻繁存檔 — Git 是你的保險

> 來源：The Pragmatic Programmer、Clean Code

**為什麼在 AI 時代更重要：**
AI 可能一次改壞很多東西。如果你沒有頻繁 commit，你就沒辦法「回到上一個能動的版本」。Replit 刪資料庫的事件就是血淋淋的教訓。

**你必須做到的：**
- 每個小功能完成就 commit 一次
- 讓 AI 在新的 branch 上工作，確認沒問題再合併
- commit message 要有意義：`feat: 新增登入功能` 而不是 `update`
- 大改動前先備份或建新分支

**對應書籍：** The Pragmatic Programmer（「用版本控制保護自己」）

---

### 6. 錯誤處理 — AI 最愛偷懶的地方

> 來源：Clean Code、Code Complete

**為什麼在 AI 時代更重要：**
AI 生成的程式碼經常把錯誤處理當作次要的事——它會讓「快樂路徑」跑得很好，但一遇到異常就崩潰或靜默失敗。

**你必須做到的：**
- 每次 AI 寫完功能，追問：「如果網路斷了會怎樣？如果使用者輸入空的會怎樣？」
- 要求錯誤訊息要具體，不是只有 "error"
- API 呼叫、資料庫操作、檔案讀寫——這三類一定要有 try-catch
- 不要讓程式「靜默失敗」，所有錯誤都要記錄或回報

**對應書籍：** Clean Code（第7章：錯誤處理）

---

### 7. 管理依賴 — 不要讓 AI 把你的專案變成套件垃圾場

> 來源：The Pragmatic Programmer、Working Effectively with Legacy Code

**為什麼在 AI 時代更重要：**
AI 很喜歡引入第三方套件來解決問題——有時候一個 10 行就能解決的事情，它會引入一個完整的函式庫。更危險的是，AI 有時候會推薦不存在的套件或已棄用的版本。

**你必須做到的：**
- 每次 AI 新增套件，問：「這個套件真的必要嗎？能不能用更簡單的方式？」
- 確認套件是否真的存在（AI 幻覺問題）
- 確認套件是否還在維護、有沒有已知漏洞
- 定期清理不再使用的依賴

**對應書籍：** The Pragmatic Programmer（「不要假設，要驗證」）

---

### 8. 理解你的系統邊界 — 知道哪些部分你不能全交給 AI

> 來源：The Mythical Man-Month、Code Complete

**為什麼在 AI 時代更重要：**
Vibe Coding 讓人容易產生「AI 什麼都能做」的錯覺。但有些事 AI 做不好，你需要知道在哪裡該收回主導權。

**AI 做得好的：**
- 產出 CRUD、UI 元件、表單驗證、資料轉換
- 寫測試、寫文件、產出樣板程式碼
- 重構和格式化

**你必須自己把關的：**
- 認證與權限設計
- 付款和金融邏輯
- 資料庫結構設計（schema）
- 第三方 API 的整合邏輯
- 效能瓶頸的診斷

**對應書籍：** The Mythical Man-Month（「沒有銀子彈」）

---

## Vibe Coding 的完整開發流程

把以上精華整合到實際的工作流程中：

```
Step 1: 定義需求 → 規劃（最重要的步驟，建議全程使用 Plan Mode）

  （完整流程共 7 步：規劃 → 行為描述 → 測試 → 實作 → 驗收 → 重構 → 收工）

  ┌──────────────────────────────────────────────────────────┐
  │ 💡 什麼是 Plan Mode？                                     │
  │                                                          │
  │ Claude Code 的「唯讀思考模式」。                            │
  │ 開啟後，AI 只能讀檔案、搜程式碼、分析架構、跟你對話，       │
  │ 但「完全不能」修改任何檔案或執行任何指令。                   │
  │                                                          │
  │ 好處：                                                    │
  │ • 零風險 — AI 不會在你還沒想清楚的時候就開始亂改            │
  │ • 更快 — 不執行工具，回應速度更快、token 消耗更少           │
  │ • 更深入 — AI 會用更多精力在分析和提問，而不是急著寫程式     │
  │ • 可審核 — AI 產出完整計畫後，你逐項確認才放行               │
  │                                                          │
  │ 怎麼開：                                                  │
  │ • 按 Shift+Tab 兩次（底部會顯示 ⏸ plan mode on）          │
  │ • 或輸入 /plan 指令（v2.1.0+）                             │
  │ • 或啟動時加 --permission-mode plan                        │
  │                                                          │
  │ 怎麼關：                                                  │
  │ • 再按一次 Shift+Tab 即可退出                              │
  │ • 退出後 AI 會再跟你確認一次才開始執行                      │
  │                                                          │
  │ 跟一般模式的差別：                                         │
  │                                                          │
  │  一般模式：你說需求 → AI 立刻開始改檔案寫程式               │
  │  Plan Mode：你說需求 → AI 只分析、只提問、只出計畫          │
  │             → 你確認 OK → 退出 Plan Mode → AI 才動手       │
  │                                                          │
  │ 白話說：就像蓋房子，Plan Mode 是畫設計圖的階段，            │
  │ 退出 Plan Mode 才是真的開始施工。                           │
  └──────────────────────────────────────────────────────────┘

  大多數失敗的 Vibe Coding 專案，不是 AI 寫錯，是你沒講清楚。
  這個步驟的目標：讓你和 AI 對「要做什麼」有完全一致的理解。

  👉 開啟 Plan Mode（Shift+Tab 兩次），然後開始：

  1a. 先把想法倒出來（你先講，不用完美）
      └─ 用白話寫下你想要什麼，就像在跟朋友描述一樣
      └─ 不用擔心格式或術語，先把腦袋裡的東西都倒出來
      └─ 範例：「我想做一個可以排程發訊息的 LINE Bot，
              使用者可以設定幾點幾分發什麼訊息給誰」

  1b. 讓 AI 來問你問題（AI 引導你想清楚）
      └─ 這個技巧的靈感來自台灣 AI 寫程式推廣大使 Mosky 劉依語
         （前 Pinkoi 架構師，20 年程式經驗，learn.mosky.tw）
      └─ 告訴 AI：「先不要寫程式，請根據我的需求問我 5 個問題，
                    幫我把沒想到的細節釐清」
      └─ 因為在 Plan Mode 裡，AI 本來就不能寫程式，
         所以它會把全部精力放在幫你「想清楚」
      └─ AI 會問出你沒想到的事：
         「如果使用者設定的時間已經過了呢？」
         「訊息發送失敗要不要重試？」
         「要支援多個使用者還是只有你自己用？」
      └─ 你不需要每個都有答案——回答「先不管」也是一種決策

  1c. 讓 AI 覆述確認（確保 AI 理解對了）
      └─ AI 會用它自己的話，簡短地把你的需求整理一遍
      └─ 然後問你：「有沒有什麼我理解錯的，或是漏掉的？」

  1d. 讓 AI 產出計畫（Plan Mode 的核心價值）
      └─ AI 會自動分析你的專案結構、現有程式碼、依賴關係
      └─ 然後產出一份完整的實作計畫，包含：
         • 要改哪些檔案
         • 執行順序和依賴關係
         • 每一步的具體做法
         • 邊界情況怎麼處理（例如：輸入是空的？時間已經過了？）
         • 出錯的時候怎麼辦（例如：網路斷了？API 沒回應？）
         • 安全考量（如果有涉及登入、付款、個資）
         • 潛在風險和替代方案
         • 使用了哪些 Design Pattern，以及為什麼選擇它們
      └─ 最後一點很重要：AI 必須明確告訴你它選了什麼架構模式。
         例如：「我選擇用 Observer Pattern 來處理事件通知，
         因為你的需求有多個元件需要同時收到狀態更新。」
         你不需要懂這些名詞，但 AI 必須解釋「為什麼這樣設計」。
         這樣未來要改架構時，你才知道當初的設計邏輯是什麼。
      └─ 複雜專案可以請 AI 把計畫存成 PLAN.md：
         「請把這份計畫寫入 PLAN.md」（退出 Plan Mode 後執行）
      └─ 你逐項審核：合理的保留，不合理的要求修改

  1e. 確認放行，退出 Plan Mode
      └─ 計畫確認後，按 Shift+Tab 退出 Plan Mode
      └─ AI 會再問你一次：「確定要開始執行嗎？」
      └─ 確認後 AI 才會開始真正動手寫程式

  ✅ 完成標準：AI 用一句話總結這個專案要做什麼，
               你確認「對，就是這個」，
               而且 AI 有一份你審核過的實作計畫。

  💡 小技巧：如果專案很小（30 分鐘內能完成），
     可以不用存 PLAN.md，Plan Mode 對話本身就是你的計畫。
     如果專案很大，建議額外產出 PLAN.md 和 ARCHITECTURE.md。

Step 2: 把計畫變成「行為描述」——你跟 AI 的合約

  跟 AI 說：
  「根據我們剛才的計畫，幫我列出這個功能的行為描述。
    用 Given-When-Then 格式，每個情境一段，包含正常跟異常的情況。」

  AI 會開始整理。

  ☕ 趁 AI 在忙，我們聊一下這是什麼：

  你跟 AI 合作，其實就是一個團隊——你是老闆，AI 是員工。
  老闆跟員工之間最怕的就是「我以為你懂了，結果你做的不是我要的」。

  行為描述就是你們之間的**合約**。
  它用一種很簡單的格式，把「系統應該怎麼反應」寫清楚：

  ```
  Scenario: 正常排程
    Given 使用者已經登入
    When  使用者設定明天早上 9:00 發送「早安」給小明
    Then  系統應該建立一筆排程記錄
    And   明天 9:00 時訊息應該被發出

  Scenario: 設定過去的時間
    Given 使用者已經登入
    When  使用者設定昨天下午 3:00 發送訊息
    Then  系統應該顯示「不能設定過去的時間」
  ```

  看到了嗎？完全是白話。你不需要懂程式，就能判斷這些描述
  是不是你要的。這就是它的價值——**你看得懂，AI 也看得懂**。

  ☕ OK，AI 整理完了，來核對：

  逐條看 AI 列出來的行為描述：
  🟢 「對，這就是我要的」→ 保留
  🔴 「不對，我沒要這個」→ 刪掉或修改
  🔴 「少了一個情況」→ 告訴 AI 補上

  ⚠️ 特別注意：範例要夠多樣
  AI 容易只從你給的一種例子去推測。
  如果你的功能要處理人名，別只給「王小明」——
  也要包含英文名、縮寫、特殊字元的情況。
  範例越多樣，AI 產出的行為描述就越完整，
  後面踩到邊界情況的機率就越低。

  全部確認後，這份行為描述就是你們的合約。
  接下來 AI 寫的測試和功能，都會以這份合約為依據。

  💡 為什麼不直接寫測試就好？
  測試的程式碼你看不懂，就算 AI 寫錯了你也發現不了。
  但行為描述是白話，你一眼就能看出對不對。
  先用你看得懂的語言確認方向，再讓 AI 轉成你看不懂的程式碼——
  這樣就不會做到最後才發現「啊，這不是我要的」。

  💡 每次都要寫行為描述嗎？
  如果功能很簡單（例如「加一個按鈕，點了跳到另一頁」），
  可以跳過這步，直接進 Step 3。
  但如果功能有「出錯時怎麼辦」「多種使用情境」，
  花兩分鐘寫行為描述，可以省你兩小時的重做。

Step 3: 產生測試

  跟 AI 說：
  「根據剛才的行為描述，幫我產生對應的測試。
    每個 Scenario 都要有測試，包含正常跟異常情況。」

  AI 會開始忙碌地工作。

  ☕ 趁 AI 在忙，我們來聊一下為什麼要先做這一步：

  你有沒有組過 IKEA 的傢俱？組完之後你一定會拉拉抽屜、
  搖搖看穩不穩，對吧？測試就是在做這件事——只是它寫一次
  就能反覆自動跑，不用每次自己手動試。

  AI 寫的程式「看起來對」不代表「真的對」。測試就是你的
  客觀裁判——跑過了就是對的，沒過就是有問題。先有裁判再
  上場比賽，才不會自己騙自己。

  而且因為我們有 Step 2 的行為描述，AI 不用猜你要測什麼——
  每一條 Given-When-Then 都會直接對應一個測試。

  ☕ OK，AI 應該跑完了，來看一下結果：

  先跑一次測試（AI 會告訴你怎麼跑，通常就是一行指令）。
  這時候測試會全部「失敗」——這是正常的！功能都還沒寫嘛。
  等 Step 4 寫完功能，測試就會一個一個變成「通過」。

  💡 我看不懂測試的程式碼怎麼辦？
  沒關係，你只需要看懂測試的「名稱」就好。
  因為是從行為描述產生的，名稱會很像白話，例如：
  「使用者設定過去的時間 → 應該顯示錯誤」
  「訊息發送成功 → 應該回傳確認」
  你可以對照 Step 2 的行為描述——一條描述對一個測試。

  💡 以後每次都要自己記得跑測試嗎？
  不用！如果你有照本指南的 CLAUDE.md 設定，裡面已經寫了
  「每完成一個子功能就自動跑測試」的規則。
  也就是說，從 Step 4 開始，AI 每做完一塊就會自己跑測試，
  你只要看結果是紅色還是綠色就好，不用自己記得跑。

  ⚠️ Debug 也要自動化驗證
  以後如果遇到 bug，修完之後一定要補一個測試來確認修好了。
  不要讓自己（或使用者）反覆手動測試來確認「這次有沒有修好」。
  跟 AI 說：「修完 bug 之後，幫我補一個測試確保這個問題不會再出現。」
  這樣下次如果有人不小心改壞了同一個地方，測試會立刻抓到。

Step 4: 讓 AI 開始寫功能（當慣老闆，看著它做）

  跟 AI 說：「根據計畫和行為描述，開始寫功能。」

  ⚠️ 如果功能會用到外部 API（例如 OpenAI、Stripe、LINE），
  先跟 AI 說：「用 chub 查一下這個 API 的最新文件再開始寫。」

  為什麼？因為 AI 的訓練資料有截止日期，它記得的 API 用法
  可能已經過時了。chub 會抓最新的官方文件，讓 AI 照著寫，
  而不是靠記憶猜。（需要先安裝：npm install -g @aisuite/chub）

  ```bash
  chub search "stripe"              # 找文件
  chub get stripe/api --lang js     # 抓最新文件
  ```

  💡 沒有安裝 chub 也沒關係，你也可以直接跟 AI 說：
  「去查一下這個 API 的官方文件再寫」——但 chub 更可靠，
  因為它的文件是社群維護的，比 AI 自己去搜尋更精準。

  Claude Code 會自己把計畫拆成好幾個小步驟，然後一步一步做。
  你不需要告訴它怎麼拆——Step 1 的計畫和 Step 2 的行為描述
  已經給了它清楚的方向，它會照著順序執行。

  而且因為 CLAUDE.md 的規則，AI 每做完一塊就會自動跑測試、
  自動存檔。你只要當慣老闆，看著它做：

  🟢 測試通過，AI 自動存檔 → 繼續看它做下一塊
  🔴 測試沒過 → AI 會自己嘗試修，修完再跑一次
  🔴 修了兩三次還是沒過？這時候就別讓 AI 繼續硬修了。
     Shift+Tab 切回 Plan Mode，跟 AI 說：
     「這個部分一直過不了，我們重新想一下做法。」
     可以回頭看 Step 2 的行為描述——是描述本身有問題，
     還是 AI 的實作方向不對？有合約在手，比較容易找到問題。

  💡 為什麼 Step 1 的計畫這麼重要？
  因為 Claude Code 自動拆步驟拆得好不好，完全取決於你的計畫
  夠不夠清楚。計畫寫得好，AI 拆的順序就合理；計畫模糊，
  AI 就會自己猜，猜錯了你才要花時間修。
  所以真正決定成敗的不是 Step 4，而是 Step 1 和 Step 2。

  ⚠️ AI 容易踩的坑（看到這些就要喊停）：
  - 環境變數沒有 .strip() — Vercel 等平台可能在值尾巴偷加換行符，
    肉眼看不到，但組 URL 或呼叫 API 時會莫名其妙壞掉。
    跟 AI 說：「所有 env 變數讀出來都要 .strip()」
  - API 回傳空物件被當「沒資料」— {} 在 Python 是 falsy，
    AI 很愛寫 `if not response` 來判斷，但空的回傳值不等於失敗。
    跟 AI 說：「用 `is None` 判斷，不要用 `not`」
  - 外部 API 錯誤被靜默吞掉 — AI 超愛寫 `except: pass`，
    出了問題你完全沒線索。
    跟 AI 說：「所有 API 呼叫的錯誤都要 log 出來，不准靜默吞掉」

  全部做完、測試都綠了？進入 Step 5。

Step 5: 驗收——讓 AI 跟你報告它做了什麼

  測試都綠了？先別急著慶祝。
  先拿出 Step 2 的行為描述，對照一下 AI 做出來的東西。
  跟 AI 說：「用白話跟我解釋一下你剛才做了什麼。」

  AI 會開始整理它的工作摘要。

  ☕ 趁 AI 在整理，我們聊一下為什麼要這一步：

  你有沒有叫過外賣，打開一看發現跟你點的不一樣？
  測試全過只能證明「東西能吃」，不能證明「是你點的那道菜」。
  AI 有時候會偷偷多做你沒要求的事，或是用火箭筒來打蚊子。

  所以這一步就是打開外賣盒，核對一下菜單。

  ☕ OK，AI 整理完了，來看它的報告：

  你不用看懂程式碼。你只要看懂它的解釋就好。
  特別注意兩件事：
  - 「出錯的時候會怎樣？」（例如網路斷了、使用者亂輸入）
  - 「有沒有多做你沒要的東西？」

  🟢 解釋合理，就是你要的 → 繼續問它下面這句
  🔴 聽起來怪怪的，跟你想的不一樣 → 切回 Plan Mode 重新想

  接著跟 AI 說：「有沒有不需要的程式碼可以刪掉？」
  AI 就像搬家時會多塞一堆「以後可能用到」的雜物的室友。
  定期幫它斷捨離，程式碼才不會變成垃圾屋。

  🔐 如果這次的功能跟安全有關（登入、付款、個資）：
  跟 AI 說：「幫我做安全檢查」
  AI 會用 security-check 技能掃描有沒有安全漏洞。
  就像出門前檢查瓦斯有沒有關——養成習慣就不會怕。

  💡 驗收時發現方向整個歪了怎麼辦？
  千萬不要直接叫 AI 改。切回 Plan Mode（Shift+Tab）先想清楚。
  迷路的時候隨便轉彎只會越轉越遠，打開地圖重新看才是正解。

Step 6: 整理一下——煮完大餐要洗碗

  跟 AI 說：「看一下目前的程式碼，有沒有可以整理的地方？
  先告訴我你打算怎麼整理，我同意了再動手。」

  AI 會開始分析程式碼。

  ☕ 趁 AI 在分析，我們聊一下為什麼要整理：

  你有沒有煮過一頓大餐？做完之後廚房一定是一團亂：
  砧板上有菜渣、鍋子疊了三層、調味料散落一地。
  如果不洗碗，下次要煮的時候就得先花半小時收拾。

  程式碼也一樣。功能做完之後趁記憶還新鮮，花幾分鐘
  整理一下，下次加新功能的時候就不用在一團亂裡面找東西。

  ☕ AI 分析完了，它會告訴你它找到了什麼：

  它通常會從這幾個方向檢查（你看名字就知道在說什麼）：
  - 📏 「這個函式做太多事了」→ 應該拆成小塊
  - 🔁 「這兩段幾乎一樣」→ 應該合成一個
  - 🏷 「這個名字看不出在幹嘛」→ 應該改名
  - 🗑 「這段沒有人在用」→ 應該刪掉

  覺得合理就跟 AI 說「OK，動手吧」。

  整理完 AI 會自動跑測試——確認打掃的時候沒有把碗打破。

  🟢 測試全過 → 整理成功，繼續！
  🔴 測試壞了 → 跟 AI 說：「整理後測試壞了，幫我退回上一步。」
     AI 會用 Git 回到整理前的狀態，不會有任何損失。

  💡 什麼時候可以跳過這步？
  改了一行文字、調了一個顏色？直接跳過沒問題。
  但如果加了一整個新功能，花兩分鐘整理一下，
  你的未來自己會感謝你的。

Step 7: 收工！存檔然後繼續下一個

  跟 AI 說：「幫我把這次的改動存檔，
  commit message 寫：feat: 完成 XXX 功能。」

  AI 會處理所有的 Git 操作，你不用管細節。

  ☕ 這裡的「存檔」其實就是 Git 的 commit + merge。
  你可以把它想成遊戲裡的「永久存檔點」——
  以後不管怎麼改，都可以回到這個點。

  🔄 存完了，然後呢？

  你剛才完成的是 Step 1 計畫裡的「一個功能」。
  看一下計畫裡還有沒有下一個功能要做：

  🟢 還有下一個功能 → 從 Step 2（行為描述）開始下一輪
     不用重新規劃——計畫在 Step 1 就列好了。

  🟢 計畫裡的功能全部做完了 → 恭喜！🎉
     想加新功能的話，回到 Step 1 開始新的一輪規劃。

  🔴 做到一半發現原本的計畫需要大改 →
     回到 Step 1，用 Plan Mode 重新想過。
     這不是失敗，這是正常的。軟體開發就是「做了才知道」。

  💡 建立好節奏很重要
  整個流程跑個兩三輪之後，你會發現 Step 2~7 越來越快，
  因為你跟 AI 之間的默契越來越好。
  真正花時間的永遠是 Step 1 的規劃——而這正是最值得花時間的地方。

─────────────────────────────────────────────────────

🔄 核心節奏：Plan ↔ Execute 的反覆切換

  整個開發流程不是一條直線，而是一個循環：

  ⏸ Plan Mode（想）  →  ▶ 執行（做）  →  ⏸ Plan Mode（檢討）
       ↑                                        │
       └────────────────────────────────────────┘

  Shift+Tab 是你最常用的按鍵。好的 Vibe Coding 開發者
  不是一直讓 AI 寫寫寫，而是不斷在「想」和「做」之間切換。

  什麼時候該切回 Plan Mode？

  ✅ 每個新功能開始前 — 先規劃再動手
  ✅ 寫到一半卡住時 — 退一步讓 AI 重新分析
  ✅ 審核發現問題時 — 先想清楚怎麼修，不要急著改
  ✅ 不確定 AI 的方向對不對時 — 停下來問，不要等它做完才發現不對

  什麼時候不需要切回 Plan Mode？

  ❌ 小修小補（改個文字、調個樣式）— 直接做就好
  ❌ AI 正在按計畫執行且一切順利 — 不用打斷它
  ❌ 只是跑測試或 commit — 這些是機械動作

  記住：Plan Mode 的成本幾乎是零（更快、更省 token），
  但跳過規劃的代價可能是整個下午的重做。
  寧可多 Plan 一次，也不要少 Plan 一次。
```

---

## 快速對照表：10 本書 × Vibe Coding 適用度

| 書名 | AI 時代適用度 | 留下的精華 | 可以略過的部分 |
|------|:---:|------|------|
| **Clean Code** | ⭐⭐⭐⭐ | 錯誤處理、命名原則、函式設計的判斷力 | 手動格式化、註解寫法、具體語法範例 |
| **The Pragmatic Programmer** | ⭐⭐⭐⭐⭐ | 幾乎全書適用——思維方式、計畫先行、版本控制、不要假設 | 少數關於手動工具鏈的章節 |
| **Refactoring** | ⭐⭐⭐ | 「何時該重構」的判斷力、程式碼的壞味道 | 手動重構的步驟（AI 可以代勞） |
| **Code Complete** | ⭐⭐⭐ | 防禦性程式設計、系統設計思維 | 大量的手動建構細節 |
| **Design Patterns** | ⭐⭐⭐ | 要求 AI 主動說明使用了哪些模式及原因 | 記憶和手動實作 23 個模式 |
| **A Philosophy of Software Design** | ⭐⭐⭐⭐⭐ | 整本書的核心——降低複雜度 | 幾乎沒有可以略過的 |
| **The Clean Coder** | ⭐⭐⭐⭐ | 專業態度、估時間、說不的能力 | 傳統職場環境的具體情境 |
| **Working Effectively with Legacy Code** | ⭐⭐⭐ | 面對「別人（或 AI）寫的爛 code」的策略 | 手動打破依賴的具體技巧 |
| **TDD: By Example** | ⭐⭐⭐⭐⭐ | 測試先行的思維——在 AI 時代這是你最重要的品質保證 | 手寫測試的具體步驟（AI 會幫你寫） |
| **The Mythical Man-Month** | ⭐⭐⭐⭐ | 軟體開發的本質不變——複雜度、溝通成本、沒有銀子彈 | 60年代的具體管理案例 |

---

## 結論

Vibe Coding 改變的是「誰來寫程式碼」，沒有改變的是「什麼讓程式碼能活下來」。

AI 幫你寫了程式碼，但你負責讓它：
- **安全**（不洩漏、不被攻擊）
- **正確**（通過測試、處理邊界情況）
- **可維護**（簡單、有結構、有版本控制）
- **可理解**（你或下一個人能看懂它在做什麼）

這四件事，沒有任何 AI 能替你負責。

---

## 進階附錄：自動化測試（Hooks）

> 💡 這段是給想更進一步的人看的。如果你覺得目前的流程已經夠用，可以跳過。

前面提到 CLAUDE.md 裡有寫「每完成一個子功能就自動跑測試」的規則。
但那是靠 AI「記得」去做——就像你跟室友說「用完廚房記得擦桌子」，
它大部分時候會做，但偶爾還是會忘。

如果你想要「保證版」——不管 AI 記不記得，每次改完檔案都一定會跑測試——
可以用 Claude Code 的 **Hooks** 功能。

### Hooks 是什麼？

Hooks 就是你設定的「自動觸發規則」。
你可以告訴 Claude Code：「每次你改完一個檔案，就自動跑一次測試。」
這就像在廚房裝了一個感應器——只要有人用了爐子，抽油煙機就自動打開。

### 怎麼設定？

在你的專案根目錄建立 `.claude/settings.json`（如果已經有了就編輯它），
加入以下設定：

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "npm test 2>&1 | tail -5"
      }
    ]
  }
}
```

這段的意思是：
- `PostToolUse` = AI 每次用完一個工具之後
- `"matcher": "Edit|Write"` = 只在 AI「編輯」或「寫入」檔案時觸發
- `"command"` = 自動執行的指令（這裡是跑測試，只顯示最後 5 行結果）

### Python 專案？

把 `npm test` 換成你的測試指令：

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "python -m pytest --tb=short -q 2>&1 | tail -10"
      }
    ]
  }
}
```

### 注意事項

- Hook 執行的指令不能太慢，不然每次改檔案都要等很久
- 建議只跑「快速測試」，完整測試留給 Step 4 驗收時跑
- 如果你的專案還沒有測試，先不用設定這個——等你跑過幾輪 Step 2 之後再來
- 設定錯了也不會壞掉，頂多就是看到錯誤訊息，刪掉設定就恢復了

💡 **CLAUDE.md 規則 vs Hooks 的差別？**
- CLAUDE.md 規則：像是跟 AI 口頭約定的守則（AI 會盡量遵守，但可能忘記）
- Hooks：像是寫死的自動化腳本（AI 沒辦法跳過，一定會執行）
- 建議新手先用 CLAUDE.md 規則就好，等你比較熟了再加 Hooks
