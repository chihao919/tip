# 🙋 常見問題 FAQ

> 把這頁想成你的 Claude Code 新手保證書 — 有問題先來這裡找，找不到再去問 AI 😄

---

## Q1. Claude Code 是免費的嗎？需要什麼方案？

簡短回答：**不是完全免費，但包含在幾個訂閱方案裡。**

就像 Netflix 一樣，你需要訂一個方案才能用。目前 Claude Code 包含在以下方案中：

| 方案 | 月費 | 適合誰 |
|------|------|--------|
| **Pro** | $20 / 月 | 個人開發者、學生、側專案 |
| **Max** | $100 或 $200 / 月 | 重度使用者、自由接案者 |
| **Team** | 按人頭計算 | 小型開發團隊 |
| **Enterprise** | 聯繫業務 | 大公司、需要合規的組織 |

如果你只是想試試看，**Pro 方案是最划算的起點** — 一個月少吃幾次大餐的錢，換一個 AI 幫你寫程式碼的夥伴 🍔→💻

> 方案詳情隨時可能更新，建議直接看 [Anthropic 官網](https://www.anthropic.com/pricing) 確認最新資訊。

---

## Q2. CLAUDE.md 跟 `.claude/settings.json` 有什麼差別？

這兩個是完全不同用途的東西，用一個比喻來說：

想像你剛請了一位新員工（AI）。

- **CLAUDE.md** 是「**職位說明書**」— 告訴這位員工他的工作方式、行為準則、你希望他怎麼溝通、用什麼程式語言風格等等。AI 會**閱讀並理解**這份文件，然後照著做。

- **`.claude/settings.json`** 是「**門禁卡系統**」— 決定這位員工可以進哪些房間（可以執行哪些指令）、哪些區域是禁區（封鎖哪些行為）。這是**技術層面的權限控制**，不是靠 AI 理解，而是系統強制執行。

```
CLAUDE.md        → AI 的行為規範（軟性規則，AI 用讀的）
settings.json    → 系統的權限設定（硬性規則，程式強制執行）
```

兩個都要設定好，才能讓 AI 既有禮貌又不會亂跑 😄

---

## Q3. 我可以在多個專案使用同一份 CLAUDE.md 嗎？

**可以！** Claude Code 支援兩個層級的 CLAUDE.md，就像公司規定分「全公司守則」和「部門規定」：

### 全域設定（適用所有專案）

```
~/.claude/CLAUDE.md
```

放在這裡的規則，**每個專案都會套用**。適合放：
- 你的個人偏好（語言風格、溝通方式）
- 通用的程式碼規範
- 你的 Git 設定、帳號資訊

### 專案設定（只適用該專案）

```
你的專案根目錄/CLAUDE.md
```

放在這裡的規則，**只有這個專案有效**。適合放：
- 這個專案的技術棧細節
- 專案特定的 coding 規範
- 這個專案的部署流程

兩個會**疊加生效**，專案層級的設定會補充（不是覆蓋）全域設定。所以你可以在全域放通用規則，每個專案再加上自己的細節，非常彈性 🎯

---

## Q4. AI 寫的程式碼有版權問題嗎？

老實說，這個問題在法律上還在**持續演進中**，沒有完全確定的答案。

目前的主流共識是：

- **你是負責人** — 你使用 AI 工具生成的程式碼，通常被視為**你的工作成果**，就像你用 Google 找到解法然後實作一樣
- **AI 本身沒有版權** — 大多數國家的法律認為版權需要有「人類創作者」，純 AI 生成的內容目前很難主張版權
- **訓練資料問題** — 這是更複雜的部分，目前有一些訴訟在處理「AI 是否侵犯了訓練資料的版權」，但這影響的是 AI 公司，不是你這個使用者

### 實用建議 💡

不管法律怎麼說，好的工程師習慣可以讓你高枕無憂：

1. **把 AI 寫的程式碼當作自己的** — 你要能讀懂、能解釋、能維護
2. **寫測試** — 測試本身就是你對程式碼的理解和把關
3. **不要盲目複製貼上** — 理解邏輯後再使用，這才算是「你的」程式碼
4. **商業敏感場景謹慎** — 如果是在嚴格的企業環境，先問清楚公司政策

> 這個領域的法律還在快速發展，建議定期關注最新動態。

---

## Q5. 怎麼知道 AI 寫的程式碼是對的？

這是最重要的問題！答案只有一個字：**測試。**

就像你不會因為計算機說「1+1=2」就完全不去確認，AI 寫的程式碼也需要驗證。

### 驗證的三個層次

**1. 讀懂它（最基本）**
不要讓 AI 寫你完全看不懂的程式碼。如果你看不懂，你就無法判斷它對不對，更別說維護了。

**2. 跑過它（基本驗證）**
執行程式，用真實的輸入測試輸出是否符合預期。

**3. 寫測試（最可靠）**
這才是根本解法。測試會幫你：
- 驗證邏輯是否正確
- 當你之後修改程式碼時，確保沒有搞壞東西
- 強迫你思考各種邊緣情況

### 配合工作流程使用

這份 FAQ 所在的 TIP 專案裡，有一份 [`vibe-coding-survival-guide.md`](./vibe-coding-survival-guide.md) 詳細介紹了「先寫測試，再叫 AI 實作」的工作流程，強烈建議搭配閱讀 🔗

> AI 是很好的程式碼**草稿**產生器，但你才是最後的品質把關人。

---

## Q6. Plan Mode 是什麼？什麼時候該用？

Plan Mode 就像是「**讓 AI 先思考、再動手**」的模式。

一般情況下，你叫 AI 做什麼，它就馬上開始做。但有時候任務比較複雜，你會希望 AI 先告訴你「我打算怎麼做」，你確認沒問題後再開始執行。這就是 Plan Mode 的用途。

### 什麼時候用 Plan Mode？

**適合用的情況 ✅**
- 任務涉及很多檔案、多個步驟
- 你對這個功能還不確定要怎麼做
- 重要的重構或架構改動，搞錯了很麻煩
- 你想在 AI 動工前先審查計畫

**不太需要用的情況 ❌**
- 簡單的一行修改
- 你已經很清楚要怎麼做，只是叫 AI 執行
- 快速的 debug

### 怎麼啟動

在 Claude Code 的對話中輸入：

```
/plan
```

或是在你的請求前面說「先告訴我你的計畫，不要動任何檔案」也有類似效果。

更詳細的使用技巧可以參考 [`vibe-coding-survival-guide.md`](./vibe-coding-survival-guide.md) 🔗

---

## Q7. 我不小心把密鑰上傳了怎麼辦？

先深呼吸 😮‍💨 這很常發生，但要**立刻行動**。

### 緊急處理（按順序做！）

**Step 1：馬上撤銷那把 Key（最重要）**

不管 git 歷史怎樣，只要 Key 還有效，就是風險。立刻去對應的服務把 Key 作廢：
- OpenAI → dashboard.openai.com
- GitHub → Settings > Developer settings > Personal access tokens
- AWS → IAM Console
- 其他服務 → 找 API Keys 或 Credentials 設定

**Step 2：從 git 歷史移除**

Key 撤銷後，你還需要把它從 git 紀錄裡清掉。這個步驟有點複雜，詳細步驟請看 [`troubleshooting.md`](./troubleshooting.md#api-key-不小心-commit-了) 中的完整教學 🔗

**Step 3：預防下次再發生**

```bash
# Add to .gitignore
.env
.env.local
*.key
secrets/
```

> 黃金原則：密鑰永遠不應該出現在程式碼裡。用環境變數（.env 檔案）存放，然後把 .env 加到 .gitignore。

---

## Q8. Skills 跟 CLAUDE.md 有什麼差別？

好問題！這兩個都是「告訴 AI 怎麼做事」的方式，但使用時機完全不同。

把它想成這樣：

- **CLAUDE.md** 是 AI 的「**個性與習慣**」— 它**一直都在**，影響 AI 的每一個回應。就像一個人的個性，不需要特別觸發，隨時都會表現出來。

- **Skills** 是 AI 的「**特殊技能**」— 需要用**特定指令觸發**才會啟動。就像你平常走路，但喊「跑！」才會開始跑。

```
CLAUDE.md   → 永遠啟用的行為規範（背景常駐）
Skills      → 用指令觸發的特定能力（按需執行）
```

### 實際例子

| | CLAUDE.md | Skills |
|---|---|---|
| **用途** | 「永遠用繁體中文回答我」 | `/security-check` 掃描密鑰 |
| **觸發方式** | 自動，無需觸發 | 手動輸入指令 |
| **適合** | 通用偏好、風格規範 | 可重複使用的特定任務 |

兩者相輔相成，不是競爭關係，而是搭配使用 💪

---

## Q9. 我要怎麼更新已安裝的 Skills？

就像手機 App 需要更新一樣，Skills 也可能有新版本，或是你想自己修改後重新安裝。

### 方法一：重新執行安裝腳本（最簡單）

```bash
cd /path/to/tip-project
bash install.sh
```

這會把最新的 Skills 複製到正確的位置，覆蓋舊版本。

### 方法二：手動複製

如果只想更新特定的 Skill：

```bash
# Skills are stored in ~/.claude/skills/
cp ./skills/your-skill.md ~/.claude/skills/your-skill.md
```

### 更新後記得重啟

```
更新完 Skills 後，重新啟動 Claude Code 才會生效。
```

就像更新了 Chrome 插件要重新整理頁面一樣，Claude Code 需要重新載入才能認識新的 Skills 🔄

---

> 還有其他問題？歡迎到 [GitHub Issues](https://github.com/chihao919/tip/issues) 提問，或是直接問 Claude Code — 它應該能回答大部分關於自己的問題 😄
